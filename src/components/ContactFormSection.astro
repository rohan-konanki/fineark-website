---
// src/components/ContactFormSection.astro
// You can add logic here if you decide to use client-side form handling
// e.g., using a React/Vue component with client:load
---
<section class="contact-form-section">
    <div class="container">
        <h2>Contact Us</h2>
        <p class="section-description">Have a question or need a personalized IT solution? Fill out the form below, and we'll get back to you promptly.</p>

        <form action="https://formspree.io/f/mldngvyg" method="POST" class="contact-form">
            <div class="form-group">
                <label for="name">Your Name *</label>
                <input type="text" id="name" name="name" required>
            </div>

            <div class="form-group">
                <label for="email">Your Email *</label>
                <input type="email" id="email" name="email" required>
            </div>

            <div class="form-group">
                <label for="company">Company</label>
                <input type="text" id="company" name="company">
            </div>


            <div class="form-group">
                <label for="message">Your Message *</label>
                <textarea id="message" name="message" rows="6" required></textarea>
            </div>

            <button type="submit" class="button primary submit-button">Send Message</button>
        </form>
    </div>
</section>

<style>
    .contact-form-section {
        background-color: white; /* Or a light background to make it stand out */
        padding: var(--spacing-lg) 0;
        text-align: center;
    }

    .contact-form-section h2 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        color: var(--color-primary);
    }

    .contact-form-section .section-description {
        font-size: 1.15rem;
        max-width: 700px;
        margin: 0 auto 2.5rem auto;
        color: var(--color-text-dark);
    }

    .contact-form {
        max-width: 600px;
        margin: 0 auto;
        background-color: var(--color-background); /* A slightly different shade */
        padding: var(--spacing-md);
        border-radius: var(--border-radius);
        box-shadow: 0 4px 15px rgba(0,0,0,0.05);
        text-align: left; /* Align form elements to the left */
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-group label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: 600;
        color: var(--color-text-dark);
        font-size: 1rem;
    }

    .form-group input[type="text"],
    .form-group input[type="email"],
    .form-group select,
    .form-group textarea {
        width: 100%;
        padding: 0.8rem;
        border: 1px solid var(--color-light-gray);
        border-radius: var(--border-radius);
        font-family: 'Lato', sans-serif;
        font-size: 1rem;
        color: var(--color-text-dark);
        background-color: white;
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }

    .form-group input:focus,
    .form-group select:focus,
    .form-group textarea:focus {
        border-color: var(--color-secondary);
        box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.25);
        outline: none;
    }

    .form-group textarea {
        resize: vertical; /* Allow vertical resizing */
    }

    .submit-button {
        width: 100%;
        padding: 1rem;
        font-size: 1.1rem;
        font-family: 'Lato', sans-serif;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        background-color: var(--color-secondary); /* Use your accent color */
    }

    .submit-button:hover {
        background-color: var(--color-secondary-hover);
    }

    @media (max-width: 768px) {
        .contact-form-section h2 {
            font-size: 2rem;
        }
        .contact-form-section .section-description {
            font-size: 1rem;
        }
    }
</style>